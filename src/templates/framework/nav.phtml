<nav>
   <ul>
      <?php foreach ($this->packet->getData('navIndexRows') as $i => $navRow) :
         $class = $navRow['isFun'] ? 'fun' : '';
      ?>
         <li>
            <a class="<?= $class ?>" href="<?= $navRow['url'] ?>">
               <?= $navRow['display'] ?>
            </a>
         </li>
      <?php endforeach ?>
      <li><a href="#resume">My Resume</a></li>
   </ul>
</nav>
