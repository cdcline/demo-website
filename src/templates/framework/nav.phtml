<nav>
   <?php foreach ($this->getPacketData('navRows') as $i => $navRow) :
      $class = $navRow['isFun'] ? 'fun' : '';
   ?>
      <div>
         <a class="<?= $class ?>" href="<?= $navRow['url'] ?>">
            <?= $navRow['display'] ?>
         </a>
      </div>
   <?php endforeach ?>
</nav>
